<h1>Followings</h1>

<ul>
  <% @followings.each do |following| %>
  <li>
    Name: <%= following.name %> | Bio: <%= following.bio %> |  <%= link_to(following.email, user_path(following)) %>
  </li>
  <% end %>
</ul>
<%= link_to "Back", user_path(@user) %>